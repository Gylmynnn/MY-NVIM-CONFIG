local options = {
   ensure_installed = { "lua", "vim", "vimdoc", "html", "css", "go", "gomod", "gosum", "svelte", "tsx", "python" },

   highlight = {
      enable = true,
      use_languagetree = true,
   },

   indent = { enable = true },
}

return options
